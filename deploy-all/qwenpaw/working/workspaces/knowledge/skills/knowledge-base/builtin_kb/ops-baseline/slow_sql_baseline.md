# 慢 SQL 通用排查基线

发现慢 SQL 告警时，先确认是不是同一条 SQL 指纹重复出现，再确认当前服务、环境和数据库实例是否一致，避免直接套用别的服务经验。

第一轮排查固定看四项：执行计划、索引命中、锁等待、实例负载。执行计划先看是否走预期索引，是否存在全表扫描；锁等待优先看长事务和阻塞链；实例负载同时看 CPU、IO 和连接数。

如果当前只命中了平台内置知识，没有命中同服务的历史工单或内部复盘，回答里应该给保守检查方向，不应该直接给出可能原因。
